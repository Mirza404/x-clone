(This project is on my GitHub. Titled xclone. It is not cloned locally on your machine, most likely) You are creating a single, comprehensive UI redesign plan for an X (Twitter) clone app. This plan will be handed off to Claude Sonnet, which will implement it with zero further access to you. Because of this, the plan must be complete and self-contained — Sonnet cannot ask you follow-up questions.

CONTEXT GATHERING (do this first, minimize raw file reads):

- Use codegraph to map the existing app's structure (components, pages, routing, shared UI elements) instead of reading files directly.

- Use rtk to compress any noisy tool/build output you generate along the way.

- I've attached a screenshot of the current UI (current.png) and the target UI (target.png) under ./ui_reference. Use it as the visual target for the redesign.

TASK:

Produce a UI redesign plan for the X clone covering:

1. Design direction — layout philosophy, spacing system, color palette, typography choices, and why (tie to the screenshot).

2. Screen-by-screen breakdown — for each major screen/component (feed, post composer, profile, nav, etc.), specify exact changes: what stays, what changes, new component structure.

3. Component-level specs — for reusable pieces (buttons, cards, avatars, etc.), give concrete states (default/hover/active/disabled) and sizing.

4. File-level implementation map — which files/components Sonnet should touch, in what order, and any shared tokens/variables to introduce first so later steps don't conflict.

5. Edge cases / responsive behavior — mobile breakpoints, empty states, loading states.

OUTPUT FORMAT:

- Plain, precise technical English — no stylized voice, no filler, no caveman phrasing in the final output.

- Structured with numbered headers so Sonnet can work through it sequentially.

- Where something is ambiguous, make a reasonable default decision and state the assumption in one line — do not leave gaps for Sonnet to guess at.

