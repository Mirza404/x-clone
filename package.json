{
  "name": "xclone",
  "version": "1.0.0",
  "description": "A clone of the popular social media platform X, previously known as twitter.",
  "main": "index.js",
  "scripts": {
    "format": "prettier --write . --ignore-unknown",
    "format:check": "prettier --check . --ignore-unknown",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "typecheck": "npm run typecheck --prefix backend && npm run typecheck --prefix frontend",
    "build": "npm run build --prefix frontend",
    "test": "npm run test:ci --prefix backend && npm run test:ci --prefix frontend",
    "deps:check": "npm audit --omit=dev --audit-level=high && npm audit --prefix backend --omit=dev --audit-level=high && npm audit --prefix frontend --omit=dev --audit-level=high",
    "deps:check:all": "npm audit --audit-level=high && npm audit --prefix backend --audit-level=high && npm audit --prefix frontend --audit-level=high",
    "check": "npm run format:check && npm run lint && npm run typecheck && npm run build && npm test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Mirza404/x-clone.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Mirza404/x-clone/issues"
  },
  "homepage": "https://github.com/Mirza404/x-clone#readme",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.6",
    "prettier": "3.9.6",
    "typescript-eslint": "8.67.0"
  }
}
